<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="tv.playlis.plugin">

    <application android:label="@string/app_name" android:icon="@mipmap/ic_launcher">
        <service android:name=".PluginService" android:exported="true">
            <intent-filter>
                <action android:name="me.targa.intent.action.PLUGIN" />
            </intent-filter>
        </service>
    </application>

</manifest>